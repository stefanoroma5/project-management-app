<% if developer_signed_in? %> 
  <div class="container my-5 text-center">
    <p> Welcome <%= current_developer.name %> </p> 
    <%= link_to "See your projects", projects_url(mode: "m"), class: "btn btn-primary my-5", role: "button" %>
    <%= button_to "Sign out", destroy_developer_session_path, method: :delete, class: "btn btn-danger" %>
  </div>
  
<% else %>
  <div class="container my-5 text-center">
    <h1>Manage yours projects</h1>
    <p>Progetto per il Laboratorio di Industria 4.0 che ha l'obiettivo di simulare un sistema di gestione di progetti informatici. Ogni progetto è composto da vari task caratterizzati da labels e possiede diversi collaboratori oltre che a un project manager</p>
    <%= link_to "Sign in", new_developer_session_url, class: "btn btn-primary my-5", role: "button" %>
  </div>
<% end %>